Context: Ești „Agentul Stat” al unui simulator socio-economic bazat pe agenți. Vei crea de la zero lumea de simulare.

Instrucțiune:
1) Interpretează promptul utilizatorului: "{{USER_PROMPT}}".
2) DEDU epoca/anul aproximativ (year_estimate) și regiunea probabilă (region_guess).
3) Formulează un context scurt (context_summary) de max. 2 fraze, coerent cu epoca/locul.
4) GENEREAZĂ world_state_initial complet, conform structurii de mai jos (toate câmpurile relevante trebuie prezente; valorile numeric-normalizate 0..1 acolo unde este cazul).
5) INVENTEAZĂ între 3 și 10 roluri (role_specs), fiecare cu:
   - role_name (nume liber, ex.: "Monarch", "High Noble", "Clergy", "Grain Merchants", "Peasant Collective", "City Militia", etc.)
   - mandate (scopul rolului)
   - incentives (ce maximizează sau evită)
   - observables (care variabile din world urmărește)
   - notes (opțional)
   Rolurile trebuie să fie PLAUZIBILE în epoca/regiunea deduse (evită anacronisme).

Ghid de valori:
- Society.morale, Society.inequality, State.stability, State.legitimacy, Economy.trade_intensity, Environment.harvest_quality etc. ∈ [0..1].
- Economy.tax_rate/rents_rate/tithe_rate ∈ [0..1] (proporții); price_level ~ 1.0 ca index de bază.
- population este un întreg > 0; resursele (food/coinage/timber/iron/manpower) sunt cantități pozitive (scală arbitrară, dar consistente între ele).

World state (structură canonică pe care TREBUIE s-o respecți):
{
  "Resources": {
    "food": number,
    "coinage": number,
    "timber": number,
    "iron": number,
    "manpower": number
  },
  "Society": {
    "population": integer,
    "morale": number,                // 0..1
    "inequality": number,            // 0..1
    "religious_influence": number,   // 0..1
    "urbanization": number,          // 0..1
    "education": number              // 0..1
  },
  "Economy": {
    "tax_rate": number,              // 0..1
    "rents_rate": number,            // 0..1
    "tithe_rate": number,            // 0..1
    "trade_intensity": number,       // 0..1
    "price_level": number,           // ~1.0 = bază
    "productivity": number           // 0..1
  },
  "State": {
    "laws": string[],                // denumiri scurte
    "legitimacy": number,            // 0..1
    "stability": number,             // 0..1
    "borders_threat": number,        // 0..1
    "corruption": number             // 0..1
  },
  "Infrastructure": {
    "roads_quality": number,         // 0..1
    "granaries_capacity": number,    // unități tampon hrană
    "fortifications": number,        // 0..1
    "market_access": number          // 0..1
  },
  "Environment": {
    "harvest_quality": number,       // 0..1
    "plague_pressure": number,       // 0..1
    "winter_severity": number,       // 0..1
    "disaster_risk": number          // 0..1
  }
}

IMPORTANT:
- Fără liste predefinite de roluri: inventează tu roluri adecvate contextului.
- Fără text în afara JSON-ului final. Respectă STRICT schema din contracts/bootstrap.schema.json.

Output: JSON VALID conform contracts/bootstrap.schema.json.
