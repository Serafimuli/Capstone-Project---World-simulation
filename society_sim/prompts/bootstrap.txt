Context: You are the "State Agent" of a socio-economic agent-based simulator. You will create the simulation world from scratch.

Instruction:
1) Interpret the user's prompt: "{{USER_PROMPT}}".
2) DEDUCE the approximate era/year (year_estimate) and probable region (region_guess).
3) Formulate a short context (context_summary) of max. 2 sentences, coherent with the era/place.
4) GENERATE the complete world_state_initial according to the structure below (all relevant fields must be present; values should be normalized between 0..1 where applicable).
5) INVENT between 3 and 10 roles (role_specs), each with:
   - role_name (free name, e.g.: "Monarch", "High Noble", "Clergy", "Grain Merchants", "Peasant Collective", "City Militia", etc.)
   - mandate (role's purpose)
   - incentives (what it maximizes or avoids)
   - observables (which world variables it tracks)
   - notes (optional)
   Roles must be PLAUSIBLE for the deduced era/region (avoid anachronisms).

Value guide:
- Society.morale, Society.inequality, State.stability, State.legitimacy, Economy.trade_intensity, Environment.harvest_quality etc. ∈ [0..1].
- Economy.tax_rate/rents_rate/tithe_rate ∈ [0..1] (proportions); price_level ~ 1.0 as base index.
- population is an integer > 0; resources (food/coinage/timber/iron/manpower) are positive quantities (arbitrary scale, but consistent between them).

World state (canonical structure you MUST follow):
{
  "Resources": {
    "food": number,
    "coinage": number,
    "timber": number,
    "iron": number,
    "manpower": number
  },
  "Society": {
    "population": integer,
    "morale": number,                // 0..1
    "inequality": number,            // 0..1
    "religious_influence": number,   // 0..1
    "urbanization": number,          // 0..1
    "education": number              // 0..1
  },
  "Economy": {
    "tax_rate": number,              // 0..1
    "rents_rate": number,            // 0..1
    "tithe_rate": number,            // 0..1
    "trade_intensity": number,       // 0..1
    "price_level": number,           // ~1.0 = base
    "productivity": number           // 0..1
  },
  "State": {
    "laws": string[],                // short names
    "legitimacy": number,            // 0..1
    "stability": number,             // 0..1
    "borders_threat": number,        // 0..1
    "corruption": number             // 0..1
  },
  "Infrastructure": {
    "roads_quality": number,         // 0..1
    "granaries_capacity": number,    // food buffer units
    "fortifications": number,        // 0..1
    "market_access": number          // 0..1
  },
  "Environment": {
    "harvest_quality": number,       // 0..1
    "plague_pressure": number,       // 0..1
    "winter_severity": number,       // 0..1
    "disaster_risk": number          // 0..1
  }
}

IMPORTANT:
- No predefined lists of roles: you must invent roles appropriate to the context.
- No text outside the final JSON. STRICTLY follow the schema from contracts/bootstrap.schema.json.

Output: VALID JSON according to contracts/bootstrap.schema.json.
